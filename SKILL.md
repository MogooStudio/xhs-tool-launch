---
name: xhs-tool-launch
description: 为 AI 技能、软件工具和独立产品制作小红书宣传图文，先展示使用效果，再介绍用途与上手方式。用于工具推广的选题、分页文案、系列海报和发布包制作；不用于普通吐槽或单独改写 README。
---

# 小红书工具图文推广

把工具能力变成读者看得懂、愿意继续翻页的演示。以具体输入和输出吸引注意，再交代工具是什么、适合谁、怎么用。交付范围包括选题、图文设计、图片、发布正文；实际发布按用户的明确授权处理。

## 判断本次交付

- 用户问“怎么宣传”：给一个推荐选题、逐页文案与设计、可直接复制的发布正文。此时无需启动出图或设置偏好文件。
- 用户说“做一套”“你来做”，或接受已有方案后要求制作：沿用已确定方向，补齐必要细节并完成图片与发布包；不用重复询问已确定的页数、配色或是否出图。
- 用户只改文案或某一页：保留已认可内容，修改对应源文件；只有影响图片时才重新生成该页。

现有资料足够就直接推进。仅在缺少工具对象、核心用途或必须精确展示的素材时，追问会影响结果的信息。准备素材不等于上传、发帖、私信、提交代码或推送仓库。

## 1. 找到可展示的效果

读取用户提供的说明、仓库文档、截图与已有使用结果，确定：目标读者、要解决的问题、一组具体输入与输出、可核实的使用入口。

优先选择真实演示。没有真实输出时，可以在授权范围内试用工具，或构造明确标注“虚构案例 / 效果示意”的例子。手写输出不能伪装成真实运行记录；重排截图内容时标注“文案摘录”，保留事实，不伪造原始界面。

选题应能用一句具体的话表达，例如“我给 AI 装了张嘴”。标题里的承诺必须能在图片中兑现。没有依据的省时比例、排名、用户量、免费或开源承诺不进入文案。公开仓库不自动意味着已授予开源许可。

平台尺寸限制、发布规则、外链限制或所谓流量规律若影响决策，查证当前官方资料；视觉比例和内容结构仅作为本次设计选择，不包装成算法保证。

## 2. 组织翻页叙事

默认从下面六页结构起步，按素材合并或删减，用户指定的数量优先：

| 页 | 唯一任务 | 应出现的内容 |
|---|---|---|
| 封面 | 让读者愿意点开 | 一句大标题、一条最有力的效果片段、工具类型小字 |
| 输入或痛点 | 让人理解演示背景 | 具体场景、原始材料或改动前状态 |
| 核心效果 | 兑现封面承诺 | 可读的输出或前后对比；把最精彩的部分放大 |
| 第二个案例 | 证明用途有变化 | 另一种输入或场景，不重复上一页的卖点 |
| 怎么控制 | 让读者想到自己的用法 | 2—3 个可复制的指令、选项或使用方式 |
| 上手入口 | 让人知道下一步 | 准确项目名、获取入口、最短步骤和一个自然的问题 |

没有第二个有效案例就减页或换成必要的上手说明。每页只承担一个任务；参数清单、终端日志和完整安装过程通常留给文档，安装本身是主题时再展开。

同步写发布正文：为什么做或使用它 → 一个具体效果 → 能用在哪 → 获取方式 → 一个与主题有关的问题。话题标签少量且相关；图片已经说清的内容不逐页重复。第一人称经历必须来自用户或已执行的过程。

完成时保存 `outline.md` 和 `post.md`。大纲包含选题、受众、事实依据或示意标签、每页准确文案和布局；正文文件明确分隔标题、可复制正文、话题及参考入口。

## 3. 定义视觉

默认采用本次验证过的“编辑批注”方向，具体规格和示例见 [references/editorial-cards.md](references/editorial-cards.md)。在选择该风格或编写出图提示词时读取。用户有品牌或视觉参考时沿用其要求，避免把所有产品都画成吐槽海报。

统一比例、字体层级、颜色、留白、页码和品牌位置，改变各页的构图。默认 3:4 竖图，关键内容离边缘约 8%，底部约 10% 留给页脚，作为版式安全余量。

封面要在缩略图中读清主标题。内容页把空间留给效果，不把说明文档缩小后塞入图片。删减文字优先于缩小字号。手绘箭头、圈注、插画只服务于阅读顺序和笑点。

## 4. 出图与校对

使用当前环境的图片生成能力；在 Codex 中优先读取并使用 `imagegen` 技能和内置工具。若不可用，完成文案与提示词并如实说明图片尚未生成，不能拿提示词冒充成品。遵循当前生成工具的调用与授权要求。

出图前，将每张图的完整最终提示词保存为 `prompts/NN-topic.md`，包括画面规格、准确文案、布局及引用图片的用途。生成封面后检查文字和视觉，再把封面作为其他页的风格参考。参考原始截图时，明确它是素材插入、内容来源还是风格参考。

封面通过后，在工具允许时分批并行生成后续页，默认每批最多 4 张；没有并行能力就顺序生成。只重试失败页，不重新生成已通过的整组。批量出图结束前持续告知已完成页数和需要修正的点。

逐张查看实际图片，校对标题、关键句、项目名、仓库名、命令符号与页码，并检查遮挡、裁切、字重、对比度和风格一致性。文件存在或尺寸正确不等于视觉校对通过。

生成图出现错字、漏字或拥挤时，精简文案或修正提示词后重生成；保留旧候选和新版提示词，不用程序涂抹覆盖文字。成功结果复制到交付目录，保留工具原始输出。处理工具返回值时只输出必要的路径和状态，不把整段图片 Base64 打印成文字。

## 5. 发布包交付

默认保存到当前工作区的 `image-cards/<topic>/`；已有同名内容时使用新版本目录，避免覆盖用户编辑。

```text
image-cards/<topic>/
  outline.md
  post.md
  prompts/01-cover.md ...
  01-cover.png
  02-setup.png ...
  <topic>-xhs.zip
```

发布 ZIP 仅放审核通过的有序图片和 `post.md`，大纲、提示词及弃用候选留在源目录。核对张数、顺序、统一比例、ZIP 可解压和所有预期文件齐全。使用生成工具的实际尺寸报告结果，不把请求尺寸当作输出尺寸。

交付时给出发布包、正文和提示词目录的链接，简要说明上传顺序、生成方式和实际发布状态。完成标准是用户能够解压、依次选图、复制标题正文；若只要求策划，则文案和分页方案完整即完成。
